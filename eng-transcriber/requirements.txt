# Core pipeline
faster-whisper>=1.1.0
ctranslate2>=4.4.0
openai>=1.40.0
yt-dlp>=2024.8.6
imageio-ffmpeg>=0.5.1
requests>=2.32.0
platformdirs>=4.2.0

# Packaging (dev only)
pyinstaller>=6.10.0

# NOTE (Windows + RTX 4060 / CUDA):
#   faster-whisper 는 CTranslate2 를 사용합니다. GPU 가속을 위해서는
#   NVIDIA cuBLAS / cuDNN 라이브러리가 필요합니다. 가장 간단한 방법:
#     pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
#   (CUDA 12 기준. 자세한 내용은 README.md 참고)
